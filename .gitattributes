# Git LFS — track PDF files uploaded through the CMS
# Netlify Large Media uses LFS to store files outside the git repo.
# Enable at: Netlify Dashboard → Site Settings → Build & Deploy → Large Media
*.pdf filter=lfs diff=lfs merge=lfs -text
*.PDF filter=lfs diff=lfs merge=lfs -text
